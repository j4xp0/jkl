// home page (server component) — temporary placeholder until the real
// shortener ui ships

export default function Home() {
  return (
    <main className="flex flex-1 items-center justify-center">
      {/* uses the dimmed text token to prove the theme pipeline works */}
      <p className="text-text-dim">jkl – under construction</p>
    </main>
  );
}
